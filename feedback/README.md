# Runtime feedback

`latest.json` is the sanitized hand-off point for the most recent Candidate diagnosis. Never include cookies, authorization headers, validation tokens or personal travel history.

The browser userscript does not write to this repository directly. Until a protected ingest service exists, update this file through the `runtime-feedback` issue template or an explicitly reviewed code-agent change.
